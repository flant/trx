VERDICT: NEEDS_CHANGES

- **blocker** — `internal/command/process_unix.go:20-27`, `internal/command/command.go:146`: cancellation sends only SIGTERM to the group. After `WaitDelay`, `os/exec` calls `cmd.Process.Kill()` on **sh only** (Go 1.26 `os/exec/exec.go:835-842`). A child that traps/ignores TERM survives outside the host lock; the source comment explicitly admits it. Escalate the group, not just the direct child.
- **should-fix** — `cmd/trx/run.go:141-146`, `internal/command/command.go:66-69`: a signal after checkout but before `Exec` still runs `onCommandStarted` under `WithoutCancel` (up to five minutes), then `Exec` refuses to start the deployment. This reports a start for a command that never ran and prolongs the lock after cancellation. Limit detached execution to reporting hooks or check `ctx.Err()` before started/skipped/success paths.
- **should-fix** — `internal/config/config.go:57-60,235-240`: `NewConfig("")` passes nil, so `trx --config ""` now returns `config path is not specified`; base invoked `_default()` and loaded `./trx.yaml`. `cmd/trx/main.go:27,37` still documents default config behavior. Retain empty-path default or reject empty flag explicitly/document it.
- **should-fix** — `internal/storage/local/local.go:126-137`: syncing temp file then renaming without fsyncing parent directory does not make rename metadata durable across a crash/power loss. State can disappear/revert despite atomic-write claim. `TestStoreSucceedTag_isAtomic` (`local_test.go:73-90`) only observes two normal sequential writes, so it does not test this failure mode.

Clean is safe for `.git` and ignored files: go-git `Clean` skips `GitDirName` and Status excludes ignored changes. Pre-release contract now matches README. `openExistingClone` preserves a clone when `originUrl` cannot be read (`client.go:220-223`). Process-group test is real for ordinary children (marker assertion), but does not cover TERM-ignoring descendants.
